{
  "name": "azaryel-flame-web",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add-android": "cd ../mobile && npx cap add android",
    "cap:copy": "cd ../mobile && npx cap copy",
    "cap:open": "cd ../mobile && npx cap open android"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.2.0"
  }
}